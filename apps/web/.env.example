# Ver .env.example en la raíz del monorepo para las instrucciones completas.
# Las variables de entorno se cargan desde la raíz — este archivo es solo referencia.

DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..."
ANTHROPIC_API_KEY=sk-ant-...
